{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.1.0"
  },
  "dependencies": {
    "apollo-server-core": "^3.11.1"
  },
  "scripts": {
    "dev:dal": "lerna run dev --scope=@chat-app/dal --stream"
  }
}
