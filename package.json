{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.1.0"
  },
  "dependencies": {
    "apollo-server-core": "^3.11.1"
  },
  "scripts": {
    "start:client": "lerna run start --scope=@chat-app/client --stream",
    "build:client": "lerna run build --scope=@chat-app/client --stream"
    "start:dal": "lerna run dev --scope=@chat-app/dal --stream",
    "migrate": "lerna run migrate --scope=@chat-app/dal --stream"
  }
}
